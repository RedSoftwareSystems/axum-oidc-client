# Google OAuth2 Configuration Example
# Copy this file to .env or .env.local and update with your Google OAuth2 credentials

# ==============================================================================
# Google OAuth2 Setup Instructions
# ==============================================================================
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the Google+ API
# 4. Go to "Credentials" → "Create Credentials" → "OAuth 2.0 Client ID"
# 5. Set application type to "Web application"
# 6. Add authorized redirect URI: http://localhost:8080/auth/callback
# 7. Copy the Client ID and Client Secret below

# OAuth2 Provider Endpoints - Google
OAUTH_AUTHORIZATION_ENDPOINT=https://accounts.google.com/o/oauth2/auth
OAUTH_TOKEN_ENDPOINT=https://oauth2.googleapis.com/token

# OAuth2 Credentials - Replace with your Google OAuth2 credentials
OAUTH_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
OAUTH_CLIENT_SECRET=your-client-secret-here

# OAuth2 Redirect URIs
OAUTH_REDIRECT_URI=http://localhost:8080/auth/callback
POST_LOGOUT_REDIRECT_URI=http://localhost:8080

# OAuth2 Scopes
OAUTH_SCOPES=openid,email,profile

# Base path for authentication routes (optional, default: /auth)
OAUTH_BASE_PATH=/auth

# Session Security
# Generate a secure key with: openssl rand -base64 64
PRIVATE_COOKIE_KEY=generate-a-secure-random-key-at-least-64-characters-long

# PKCE Code Challenge Method
# Options: S256 (recommended) or plain
CODE_CHALLENGE_METHOD=S256

# Server Configuration
SERVER_HOST=127.0.0.1
SERVER_PORT=8080

# Optional: Custom CA Certificate
# Uncomment and set path if you need a custom CA certificate
# CUSTOM_CA_CERT=/path/to/your/ca.pem

# ==============================================================================
# Important Notes for Google OAuth2
# ==============================================================================
#
# 1. Google does NOT support OIDC logout (end_session_endpoint)
#    - Do NOT set OAUTH_END_SESSION_ENDPOINT for Google
#    - Use DefaultLogoutHandler in your code
#
# 2. Client ID format: xxxxx.apps.googleusercontent.com
#
# 3. Scopes:
#    - openid: Required for OpenID Connect
#    - email: Access to user's email address
#    - profile: Access to user's profile information
#
# 4. Redirect URI must match exactly:
#    - Must be registered in Google Cloud Console
#    - Include protocol (http:// or https://)
#    - Include port if not default (80/443)
#
# 5. For production:
#    - Use HTTPS for redirect URIs
#    - Generate strong PRIVATE_COOKIE_KEY
#    - Store credentials securely (not in .env files)
#
# ==============================================================================
# Example with Real Format (Masked Values)
# ==============================================================================
# OAUTH_CLIENT_ID=123456789012-abcdefghijklmnopqrstuvwxyz123456.apps.googleusercontent.com
# OAUTH_CLIENT_SECRET=GOCSPX-abcdefghijklmnopqrstuvwxyz

